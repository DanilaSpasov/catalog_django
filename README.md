# Catalog Django Project

**catalog_django** — Django-проект, в котором реализовано веб-приложение с каталогом товаров и блогом. Проект включает работу с базой данных PostgreSQL, использование Class-Based Views (CBV), полноценный CRUD для товаров и блоговых записей, административную панель, а также инструменты для наполнения базы данных тестовыми данными.

---

# Технологии и стек

* **Фреймворк:** Django 6
* **Язык программирования:** Python 3
* **СУБД:** PostgreSQL
* **ORM:** Django ORM
* **Шаблоны:** Django Templates + Bootstrap 5
* **Контроль версий:** Git / GitHub
* **Аутентификация:** Django Authentication System
* **Система прав доступа:** Django Permissions и Groups
* **Пользовательская модель:** AbstractUser
* **Отправка email:** Django Email Backend
* **Кэширование:** Redis

---

# База данных

Проект использует PostgreSQL.

Настройки подключения задаются в файле `settings.py` через переменные окружения.

Для работы необходимо:

1. установить зависимости проекта;
2. создать файл `.env` по примеру `.env_example`;
3. заполнить параметры подключения к базе данных и `SECRET_KEY`;
4. настроить параметры для отправки email (EMAIL_BACKEND, EMAIL_HOST, EMAIL_PORT, EMAIL_USE_TLS, EMAIL_USE_SSL, EMAIL_HOST_USER, EMAIL_HOST_PASSWORD);
5. настроить параметры для Redis кэширования (CACHE_BACKEND, CACHE_LOCATION).


---

# Структура проекта

Проект состоит из трех приложений:

* **catalog** — каталог товаров;
* **blog** — блог с поддержкой публикации статей.
* **users** — приложение для работы с пользователями и аутентификацией.

---

# Функциональность

## Каталог товаров

Реализовано:

- Отображение списка товаров
- Страница детального просмотра товара
- Отображение изображения, цены и описания
- Связь товаров с категориями
- Полный CRUD для товаров
- Отображение товаров по категории с фильтрацией
- Выпадающее меню с категориями в навигации
- Привязка товара к владельцу (авторизованному пользователю)
- Проверка прав владельца при редактировании и удалении товара
- Разграничение прав доступа для модераторов продуктов
- Управление публикацией товаров
- Создание товара через Django Forms
- Редактирование товара
- Удаление товара
- Валидация названия и описания на наличие запрещённых слов
- Проверка цены на отрицательное значение
- Стилизация форм с использованием Bootstrap 5
- Кэширование страницы деталей товара с использованием Redis
---

## Блог

Для приложения `blog` реализован полный CRUD.

Возможности:

* просмотр списка статей;
* просмотр отдельной статьи;
* создание статьи;
* редактирование статьи;
* удаление статьи;
* загрузка изображения;
* отображение количества просмотров;
* отображение только опубликованных статей;
* автоматическое увеличение количества просмотров;
* после редактирования пользователь перенаправляется обратно на страницу статьи.

---

# Пользователи и аутентификация

Для работы с пользователями создано отдельное приложение `users`.

Реализована собственная модель пользователя на основе `AbstractUser`.

Возможности:

- регистрация пользователя по электронной почте;
- авторизация по электронной почте и паролю;
- отправка приветственного письма после регистрации;
- хранение дополнительной информации о пользователе.

Дополнительные поля пользователя:

- `avatar` — изображение профиля;
- `phone` — номер телефона;
- `country` — страна пользователя.

В качестве основного поля авторизации используется электронная почта.

---

## Ограничение доступа

Для страниц управления товарами реализована проверка аутентификации и прав доступа пользователей.

Доступ разрешен только авторизованным пользователям:

- создание товара;
- редактирование товара;
- удаление товара.

Страница просмотра списка товаров остается доступной всем пользователям.

Для ограничения доступа используется `LoginRequiredMixin`.

Дополнительно реализована система владельцев товаров:

- при создании товара автоматически сохраняется владелец (`owner`) — текущий авторизованный пользователь;
- редактирование товара доступно только его владельцу;
- удаление товара доступно владельцу или пользователю с правами модератора продуктов.

Проверка прав доступа реализована в представлениях через переопределение метода `dispatch`

Для защиты операций используется проверка владельца объекта и наличия необходимых разрешений пользователя.

---

# Модели

## Category

Поля:

* `title` — название категории;
* `description` — описание категории.

---

## Product

Поля:

* `name` — название товара;
* `description` — описание;
* `image` — изображение;
* `category` — категория (`ForeignKey`);
* `price` — стоимость;
* `created_at` — дата создания;
* `updated_at` — дата изменения;
* `owner` — владелец товара (`ForeignKey`);
* `is_published` — статус публикации товара.
---

## Blog

Поля:

* `title` — заголовок;
* `content` — содержимое статьи;
* `preview_image` — изображение статьи;
* `created_at` — дата создания;
* `is_published` — признак публикации;
* `views_count` — количество просмотров.

---

## User

Пользовательская модель создана на основе `AbstractUser`.

Поля:

* `email` — электронная почта пользователя (используется для авторизации);
* `password` — пароль пользователя;
* `avatar` — изображение профиля;
* `phone` — номер телефона;
* `country` — страна пользователя.

---

# Дополнительная логика

Для приложения блога реализованы следующие возможности:

### Увеличение количества просмотров

При открытии страницы статьи автоматически увеличивается счетчик просмотров.

Реализовано переопределением метода:

```
get_object()
```

---

### Фильтрация статей

На странице списка отображаются только опубликованные статьи.

Используется переопределение:

```
get_queryset()
```

---

### Перенаправление после редактирования

После успешного редактирования пользователь автоматически перенаправляется на страницу просмотренной статьи.

Используется:

```
get_success_url()
```

Для каталога товаров.

---

### Валидация формы

Для формы создания и редактирования товаров реализована собственная валидация (`ProductForm`).

Проверяется:

* отсутствие запрещённых слов в названии и описании товара (без учёта регистра);
* корректность стоимости товара — цена не может быть отрицательной.

---

### Владелец товара и права доступа

Для модели `Product` реализована связь с пользователем через поле `owner = ForeignKey(User)`

При создании нового товара владелец устанавливается автоматически — им становится текущий авторизованный пользователь.

Добавлена система разрешений для управления товарами:

- владелец может редактировать свои товары;
- владелец может удалять свои товары;
- модератор продуктов может удалять любые товары;
- модератор продуктов имеет право отменять публикацию товаров.

Проверка прав выполняется как в контроллерах, так и в шаблонах.


---

# Административная панель

В административной панели зарегистрированы модели:

* Category;
* Product;
* Blog;
* User.

Для моделей настроены:

* отображаемые поля (`list_display`);
* ссылки на объекты (`list_display_links`);
* поиск (`search_fields`);
* фильтрация (`list_filter`).

Для модели пользователя отображаются:

* электронная почта;
* имя и фамилия;
* статус сотрудника;
* статус активности пользователя.

---

# Фикстуры

Для быстрого наполнения базы данных подготовлены фикстуры.

Загрузка:

```
python manage.py loaddata categories.json
python manage.py loaddata products.json
```


Также реализована пользовательская management-команда для автоматической загрузки тестовых данных:
```
python manage.py add_test_products
```

Команда выполняет:

- удаление существующих объектов моделей `Product` и `Category`;
- загрузку категорий из фикстуры `categories.json`;
- загрузку товаров из фикстуры `products.json`.



Также подготовлена фикстура для групп и разрешений пользователей:
```
python manage.py create_moderator_group
```
Сохраняются:

- группа "Модератор продуктов";
- права группы;
- разрешение `can_unpublish_product`.
---

# Запуск проекта

Установить зависимости:

```
poetry install
```

Активировать виртуальное окружение:

```
poetry shell
```

Применить миграции:

```
python manage.py migrate
```

Запустить сервер:

```
python manage.py runserver
```

---

---

# Аутентификация

Для работы с пользователями реализованы:

## Регистрация

Пользователь может создать учетную запись, указав:

- электронную почту;
- пароль.

После успешной регистрации пользователю отправляется приветственное письмо.

## Авторизация

Вход в систему осуществляется по:

- электронной почте;
- паролю.

После входа пользователь получает доступ к функциям управления товарами.

---

# Доступные страницы

После запуска проекта доступны маршруты:
* /home/ - главная страница
* /contacts/ - страница контактов
* /admin/ - панель администратора Django
* /products_by_category/<category_id>/ - товары по категории
* product/<id>/ - страница товара
* /blog/ - список статей
* /blog/<id>/ - просмотр статьи
* /blog/create/ - создание статьи
* /blog/<id>/update/ - редактирование статьи
* /blog/<id>/delete/ - удаление статьи
* /users/register/ - регистрация пользователя
* /users/login/ - авторизация пользователя
* /users/logout/ - выход из аккаунта

---

# Используемые возможности Django

В проекте используются:

* Class-Based Views;
* Django ORM;
* шаблоны Django;
* наследование шаблонов;
* Bootstrap 5;
* работа с изображениями (`ImageField`);
* загрузка медиафайлов;
* миграции;
* административная панель;
* работа с PostgreSQL.
* Django Forms (`ModelForm`);
* пользовательская валидация форм (`clean`, `clean_price`);
* кастомизация виджетов форм через `__init__`;
* Django Authentication System;
* создание собственной модели пользователя через `AbstractUser`;
* формы регистрации и авторизации;
* ограничение доступа через `LoginRequiredMixin` и `login_required`;
* отправка email пользователю после регистрации.
* Django Groups и Permissions;
* создание пользовательских разрешений (`permissions`);
* проверка прав пользователей через `has_perm`;
* управление доступом на уровне объектов;
* автоматическое назначение владельца объекта через `form_valid`;
* создание пользовательских management-команд для автоматизации работы с данными;
* проверка доступа через `dispatch`;
* Context Processors для передачи данных в шаблоны;
* кэширование страниц с использованием `@cache_page`;
* сервисный слой для бизнес-логики (`services.py`).

---

# Контакты

Email: **spasov2000@mail.ru**
